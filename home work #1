#include <iostream>
using namespace std;

int main() {
	setlocale(LC_ALL, "ru");   
	int num, res = 1;
	cin >> num;
	for (int i = 1; i <= num; ++i) {
		res *= i;
	}
	cout << "the factorial of " << num << " will be: " << res;
		
	return 0;
}
