#include <iostream>
using namespace std;

int main() {
    //setlocale(LC_ALL, "ru");
    srand(time(0));
    int num;
    cin >> num;
    for (int i = 1; i <= num; i++) {
        char sim = 48 + rand() % 90;
        cout << sim;
    }
    return 0;
}
