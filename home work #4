#include <iostream>
using namespace std;

int main() {	
	int num, res = 0;
	cin >> num;
	for (int i = 1; i < num; i++) {
		if (num % i == 0) {
			res += i;
		}
	}

	if (res == num) 
		cout << num <<" - is a perfect number!";	
	else 
		cout << num << " - is not a perfect number!";

	return 0;
}
